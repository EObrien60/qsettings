{
  "name": "qsettings",
  "version": "0.1.0",
  "private": true,
  "description": "Boring, typed, scoped configuration service for OBH SaaS products.",
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
