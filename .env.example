# Postgres connection string for the SaaS database that owns the platform.* tables
DATABASE_URL=postgres://postgres:postgres@localhost:5432/qsettings_dev

# Logical product/service name (used in logs). Default: settings.
SETTINGS_SOURCE=qhaul

# debug | info | warn | error (default info)
SETTINGS_LOG_LEVEL=info
